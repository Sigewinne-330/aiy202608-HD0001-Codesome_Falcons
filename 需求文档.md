# 核心功能需求 (Functional Requirements)

### 模块一：全模态 DDL 捕获网 (Input & Capture)
| 功能名称 | 详细描述 | 交互/技术实现要求 | 优先级 |
| :--- | :--- | :--- | :--- |
| **1.1 Web 内嵌 Agent 基础录入** | 网页提供侧边栏 ChatUI。支持用户输入文本、点击麦克风发送语音、拖拽/粘贴图片（聊天截图/课件）。Agent 识别提取：任务、时间(DDL)、地点，并在网页主视图的日历上**实时生成**。 | **前端**：Web Audio API (语音录制)，拖拽上传，WebSocket 或 SSE 实现流式对话。<br>**AI 端**：多模态大模型抽取 JSON 结构数据。<br>**亮点**：对话确认后，右侧日历出现动画效果添加事件。 | **P0** |
| **1.2 响应式日历底座** | 网页主视图，提供日/周/月视图，展示已识别的 DDL 和子任务。支持拖拽任务块修改时间，双击修改详情。 | **前端**：引入 FullCalendar 或类似日历组件。支持拖拽 (Drag & Drop) 交互。 | **P0** |
| **1.3 ManageBac 嗅探插件** | 浏览器插件（Chrome Extension），点击插件按钮即可抓取当前 Canvas/ManageBac 网页的数据，并自动同步到当前登录的 Web 应用账户中。 | **前端**：DOM 解析，提取特定 HTML 元素的数据，通过 API 发送给后端。 | **P1** |

### 模块二：双核长程任务拆解引擎 (Process & RAG)
| 功能名称 | 详细描述 | 交互/技术实现要求 | 优先级 |
| :--- | :--- | :--- | :--- |
| **2.1 泛用型逆向任务拆解** | 在 Web 对话框中，用户输入大型任务（如“写3000字论文”），AI 从 DDL 倒推生成多节点子任务。 | **AI 端**：Prompt 设定“逆向排期”。<br>**前端**：在日历上以不同颜色/虚线框展示推荐的子任务排期，用户点击“确认”后实锤落地。 | **P0** |
| **2.2 IB 学科专家知识库 (RAG)** | 用户在对话框输入“我要开始写历史 EE”时，Agent 检索本地知识库，给出符合 IBO 官方指南的专业级周度步骤拆解。 | **AI 端**：搭建 RAG 流程。需提前将 IBO 官方指南 PDF 向量化并存入 Vector DB。 | **P1** |
| **2.3 碎片化 CAS 追踪器** | 当用户登录 Web 页面时，若检测到本周无 CAS 记录，Agent 会主动在侧边栏弹出气泡框询问：“这周去辩论赛了吗？用语音跟我说说”。 | **前端**：系统状态检测，触发主动对话 (Proactive Chat)。<br>**后端**：AI 语音转文本 + 摘要，存入 CAS 日志库。 | **P2** |

### 模块三：动态精力池管理 (Algorithm & Scheduling)
| 功能名称 | 详细描述 | 交互/技术实现要求 | 优先级 |
| :--- | :--- | :--- | :--- |
| **3.1 智能排雷与冲突规避** | AI 在分配子任务时，会读取数据库中该用户的已有日程。自动避开排满的日期，将任务见缝插针地安排在日历的“白色空闲区”。 | **后端/算法**：基于时间区间的冲突检测 (Collision Check) 算法。 | **P1** |
| **3.2 极限状态负载均衡** | Web 端检测到某周任务密度过高（“地狱周”），Agent 主动弹出警告，并提供“平滑化一键排期”按钮，将大任务打散前置到上周。 | **后端**：计算每日“精力消耗值”(如按任务时长计算)。超阈值则触发警报。<br>**AI 端**：重新生成排期 JSON。 | **P2** |

### 模块四：有温度的督导员 (Interaction & Persona)
| 功能名称 | 详细描述 | 交互/技术实现要求 | 优先级 |
| :--- | :--- | :--- | :--- |
| **4.1 戏精/幽默催收机制** | 根据 Persona 设定，通过 Web 页面右下角的弹窗消息 (Toast) 或浏览器 Push 通知，幽默催促即将到期的 DDL。 | **AI 端**：System Prompt 设定性格。生成文案。<br>**前端**：Web Notifications API 或内部弹窗系统。 | **P0** |